# Build log

## 2026-09-21 — Phase 0
Empty directory inspected. Blender 4.4.0 (05377985c527), Python 3.14.6, Node 26.4.0, npm 11.17.0 found on macOS. No Git author configured. Initialized Git; commits will require a genuine author identity. Public competitor/API/licensing research completed before implementation. Working name remains provisional. Plan: stdlib host Python, disposable Blender processes, JSON IR, GLB, TypeScript/React/Three/Vite. Local 4.4 is a tested target candidate, not an LTS claim. No tests yet. Open: verify exporter mapping and obtain LTS runtime if practical.

## Phase 1 — actual end-to-end slice
Generated the instrument enclosure pair; Blender 4.4 background startup crashes in the execution sandbox's Metal detection, but runs outside it. Export tested with real evaluated meshes and GLB extras. Host extraction 0.91 s for A in the first smoke run. Viewer TypeScript/build passed. Preview intentionally uses untextured material colors; copied evaluated meshes prevent current-frame/export evaluation disagreement. First CLI comparison: 1 addition, 1 removal, 7 modified, 10 unchanged, 4 unresolved entities. Golden summary records the designed seven edits (housing, panel, rail, lens, controller, camera, light), not an assumed number. Core: 15 tests passed; 5 viewer utility tests passed. Browser verification pending. Source files have not been saved by extraction.

## Phase 2–4 — semantic depth, identity and interaction
Real integration passed: fixture freshly generated, autoexec sentinel absent, source hashes unchanged, repeat snapshots identical, every mesh GLB ID mapped. Real Chrome test first failed because single-material meshes were converted to arrays without geometry groups; geometry was invisible despite successful loading. Preserving single/array material shape fixed rendering and raycast selection; browser test then passed. Screenshot captured from the real viewer. Added subfield presentation to avoid repeating stable modifier/material values in the inspector. Hardened topology hashes with isolated vertex counts and preserved relationship reference IDs even when display names coincide. Example goldens will be regenerated for those explicit protocol details, not to mask a semantic test failure. No semantic summary change expected.

## Phase 5 — hardening and compatibility
24 Python tests now pass on both installed Blender 4.4.0 and official Blender 4.5.14 LTS. Downloaded the latter to temporary storage, verified SHA-256 65134d9b07b20e2fa8d3c9e44f6f44ffb5c9774dd521b95f50387310241ca170, mounted read-only, and ran the real suite without replacing the installed application. Initial safety test expected macOS /var paths literally instead of resolved /private/var paths; corrected the test to assert the actual normalized argument. Golden test now compares canonical IR rather than unrounded in-memory displacement floats; reviewed serialized expectations did not change for this correction. Six viewer logic tests and final Chrome interaction test pass. Scale fixture: 420 semantic entities per side, matching about 1.013 s, each Blender process about 1.35 s. Current runtime/source dependency notices included. Public name registry checks found no exact GitHub/PyPI/npm entry, but name remains provisional. No Git author configured; no fabricated commit identity.

## Phase 6 — local release candidate
Built wheel and source archive. Distribution audit confirms bundled viewer, GPL/MIT notices, viewer source in sdist and no private notes. Installed wheel without dependencies into a fresh /private/tmp virtual environment; from outside checkout it compared actual fixture inputs and produced the expected 1/1/7/10/4 report. Clean extracted source completed npm ci and viewer rebuild. Public relative documentation links checked; all six ignored private learning documents present. Final readability formatting is development-only and does not change runtime dependencies. README compatibility now names the two actually tested Blender versions. Publishing remains manual: configure genuine Git identity, settle public naming, configure security reporting, run hosted workflows, then explicitly authorize publication. No remote, push, tag or fabricated community activity created.

Final code review added a direct exact-observation stage for identical captured states of the same source before heuristic matching, with a dedicated pure-core regression test. Canonical demo goldens remain unchanged. Final Python suite: 25 passed on installed Blender 4.4.0; earlier 24-test suite including the same real extraction integration passed on 4.5.14 LTS. Ruff baseline checks and compile validation pass. The LTS test image has been unmounted. Public files staged for review; private notes verified ignored.

Fresh Git-checkout audit caught a build-script edge case missed by the source archive (which already contains viewer assets): package_viewer.py assumed its generated output directory existed. It now removes that directory only when present, then creates it from the build. Verification uses a tracked-files-only copy so generated directories cannot mask missing setup steps.

Tracked-files-only checkout verification passed: npm ci followed by scripts/package_viewer.py creates the missing generated directory and builds the viewer/notices successfully. The existing final demo directory is outputs/demo-v0.1, approximately 2.3 MiB.

## Private GitHub publication
At the user's explicit request, created scapintobias/semantic-change-explorer with private visibility. Verified GitHub identity: Tobia Scapin (scapintobias); use the account's numeric-ID noreply address for repository-local commit attribution. Initial publication includes source, tests, documentation and generated public example JSON/screenshot; private-notes, local reports, binaries, dependencies and distributions remain excluded. This is a private repository publication, not a public package release or tag.
